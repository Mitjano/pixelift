rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow all operations - Firebase Admin SDK will handle authorization on backend
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
